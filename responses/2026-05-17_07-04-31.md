## Prompt
i have two models in my project root.
@Qwen3.5-0.8B-Q4_K_M.gguf , @Qwen3.5-4B-Q4_K_M.gguf
wire this removing the current models.
Use this.
Delete the existing models

## Response

Wired the two new Qwen3.5 GGUFs in place of the old `mlabonne_*` models.

### Mapping
- chat (preload, n_ctx 4096) → `models/Qwen3.5-0.8B-Q4_K_M.gguf`
- tool (preload, n_ctx 2048) → `models/Qwen3.5-4B-Q4_K_M.gguf`
- memory.extraction_server → `models/Qwen3.5-4B-Q4_K_M.gguf`

### Changes
1. Moved both GGUFs from project root into `models/`.
2. `config.yaml`: updated `models.chat.path`, `models.tool.path`, `memory.extraction_server.model_path`.
3. `core/bootstrap/settings.py`: updated `ModelSettings` defaults (dataclass + `from_config` fallbacks).
4. `core/model_manager.py`: updated fallback paths in `_profiles` dict.
5. `scratch/test_llm.py`: updated stale path.
6. Deleted `models/mlabonne_Qwen3-1.7B-abliterated-Q4_K_M.gguf` and `models/mlabonne_Qwen3-4B-abliterated-Q4_K_M.gguf`.
7. `docs/testing_guide.md`: added Modification Log row dated 2026-05-17 with a Verify command.

### Verification
```
$ python -c "from core.config import ConfigManager; c=ConfigManager(); c.load(); print(c.get('models.chat.path')); print(c.get('models.tool.path')); print(c.get('memory.extraction_server.model_path'))"
models/Qwen3.5-0.8B-Q4_K_M.gguf
models/Qwen3.5-4B-Q4_K_M.gguf
models/Qwen3.5-4B-Q4_K_M.gguf
```

### Note on docs
Docs still mentioning the old model filenames (descriptive history, not active wiring): `SETUP_GUIDE.md`, `SETUP_GUIDE_WINDOWS.md`, `docs/jarvis_vs_friday_comparative_report.md`, `docs/memory_management_strategy.md`, `docs/intent_routing_testing_guide.md`, `docs/manual_testing_guide.md`, `docs/phasewise_implementation_plan.md`. Not updated — let me know if you want them rewritten.
