python3 modules/voice_io/clap_detector.py --stop

use the above comamand to turn off clap detector.

python3 modules/voice_io/clap_detector.py --start
 
use the above command to start the clap_detector.

Detector logs:
- `logs/clap_detector.log`
- `logs/clap_launch.log`
